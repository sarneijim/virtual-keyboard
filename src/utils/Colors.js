const whiteColor = 0xffffffff
const lightBlue = 0xff748d9a
const darkBlue = 0xff05223e

const bgColor = lightBlue
const headerBgColor = darkBlue
const inputColor = darkBlue
const inputBgColor = whiteColor
const keyboardBgColor = darkBlue
const keyboardColor = darkBlue
const focusKeyboardColor = whiteColor

export {
  bgColor,
  headerBgColor,
  inputColor,
  inputBgColor,
  keyboardBgColor,
  keyboardColor,
  focusKeyboardColor,
}
